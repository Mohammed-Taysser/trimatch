# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
coverage/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example
# CI-only placeholders (no secrets) — committed so the CI env has one source
!.env.ci

# logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# test artifacts
.nyc_output/
junit.xml

# generated API artifacts (ADR-0003)
docs/api/

# browser-verification artifacts
.playwright-mcp/
